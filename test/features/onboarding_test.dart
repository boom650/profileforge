import 'package:flutter_test/flutter_test.dart';
import 'package:profileforge/features/onboarding/presentation/onboarding_screen.dart';

void main() {
  test('onboarding screen can be instantiated', () {
    // Compile-check: ensure constructor signature matches
    expect(OnboardingScreen, isA<Type>());
  });
}