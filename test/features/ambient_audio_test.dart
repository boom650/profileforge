import 'package:flutter_test/flutter_test.dart';
import 'package:profileforge/features/timer/presentation/ambient_audio_panel.dart';

void main() {
  test('ambient audio panel can be instantiated', () {
    // Compile-check: ensure constructor is accessible
    expect(AmbientAudioPanel, isA<Type>());
  });
}