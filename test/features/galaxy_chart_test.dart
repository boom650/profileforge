import 'package:flutter_test/flutter_test.dart';
import 'package:profileforge/core/widgets/galaxy_chart.dart';

void main() {
  test('galaxy chart can be instantiated', () {
    // Compile-check: ensure constructor is accessible
    expect(GalaxyChart, isA<Type>());
  });
}