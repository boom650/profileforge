# No build.yaml needed — drift_dev and freezed builders work
# with their defaults on this project. Build config was
# removed because the `include`/`generate_for` keys are
# not supported at this builder nesting level, and CI
# passes without any overrides.
